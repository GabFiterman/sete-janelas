services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: sete-janelas
    init: true
    ports:
      - '5173:5173'
    volumes:
      - .:/app
      - /app/node_modules
